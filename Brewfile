# docker-completion conflicts with formula docker (both ship bash_completion.d/docker).
# make setup unlinks docker-completion before brew bundle.
brew "docker"
brew "kubectl"
brew "helm"
brew "k3d"

