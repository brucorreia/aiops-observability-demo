# Local Argo CD. Each workload is a separate Application under infra/apps/<app>.
dex:
  enabled: false
notifications:
  enabled: false
configs:
  params:
    server.insecure: true
  cm:
    timeout.reconciliation: 15s
    timeout.reconciliation.jitter: 5s
